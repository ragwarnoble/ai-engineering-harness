[project]
name = "ai-engineering-platform"
version = "0.1.0"
description = "A reusable engineering platform for building, testing, evaluating, and operating AI, data, and software systems."
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "pytest-cov>=5",
    "ruff>=0.8",
    "mypy>=1.13",
    "PyYAML>=6.0",
    "types-PyYAML>=6.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]

[tool.mypy]
python_version = "3.12"
strict = true

[tool.coverage.run]
source = ["src/harness"]

[tool.coverage.report]
fail_under = 85
show_missing = true

[project.scripts]
harness = "harness.cli:main"
